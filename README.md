# WebAssist

A simple Django project to store and list links, with admin CRUD, search, sorting, and pagination.

## Setup

1. **Clone**  
   ```bash
   git clone https://github.com/moroshani/webassist.git
   cd webassist
